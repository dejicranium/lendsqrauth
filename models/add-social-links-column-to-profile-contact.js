'use strict';
module.exports = (sequelize, DataTypes) => {
  const add - social - links - column - to - profile - contact = sequelize.define('add-social-links-column-to-profile-contact', {
    id: DataTypes.INTEGER
  }, {});
  add - social - links - column - to - profile - contact.associate = function(models) {
    // associations can be defined here
  };
  return add - social - links - column - to - profile - contact;
};