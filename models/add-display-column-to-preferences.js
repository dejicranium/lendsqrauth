'use strict';
module.exports = (sequelize, DataTypes) => {
  const add - display - column - to - preferences = sequelize.define('add-display-column-to-preferences', {
    id: DataTypes.INTEGER
  }, {});
  add - display - column - to - preferences.associate = function(models) {
    // associations can be defined here
  };
  return add - display - column - to - preferences;
};