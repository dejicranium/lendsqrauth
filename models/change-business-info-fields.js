'use strict';
module.exports = (sequelize, DataTypes) => {
  const change - business - info - fields = sequelize.define('change-business-info-fields', {
    id: DataTypes.INTEGER
  }, {});
  change - business - info - fields.associate = function(models) {
    // associations can be defined here
  };
  return change - business - info - fields;
};