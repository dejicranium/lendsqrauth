'use strict';
module.exports = (sequelize, DataTypes) => {
  const add - next -
    try -to - collection - schedules = sequelize.define('add-next-try-to-collection-schedules', {
      id: DataTypes.INTEGER
    }, {});
  add - next -
    try -to - collection - schedules.associate = function(models) {
      // associations can be defined here
    };
  return add - next -
    try -to - collection - schedules;
};