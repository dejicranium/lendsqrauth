'use strict';
module.exports = (sequelize, DataTypes) => {
  const delete - active - disabled - flags = sequelize.define('delete-active-disabled-flags', {
    id: DataTypes.INTEGER
  }, {});
  delete - active - disabled - flags.associate = function(models) {
    // associations can be defined here
  };
  return delete - active - disabled - flags;
};