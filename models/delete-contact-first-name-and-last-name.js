'use strict';
module.exports = (sequelize, DataTypes) => {
  const delete - contact - first - name - and - last - name = sequelize.define('delete-contact-first-name-and-last-name', {
    id: DataTypes.INTEGER
  }, {});
  delete - contact - first - name - and - last - name.associate = function(models) {
    // associations can be defined here
  };
  return delete - contact - first - name - and - last - name;
};