'use strict';
module.exports = (sequelize, DataTypes) => {
  const add - bvn - to - profile = sequelize.define('add-bvn-to-profile', {
    id: DataTypes.INTEGER
  }, {});
  add - bvn - to - profile.associate = function(models) {
    // associations can be defined here
  };
  return add - bvn - to - profile;
};