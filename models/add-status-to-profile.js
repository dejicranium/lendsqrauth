'use strict';
module.exports = (sequelize, DataTypes) => {
  const add - status - to - profile = sequelize.define('add-status-to-profile', {
    id: DataTypes.INTEGER
  }, {});
  add - status - to - profile.associate = function(models) {
    // associations can be defined here
  };
  return add - status - to - profile;
};