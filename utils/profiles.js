const models = require('mlar')('models');
const q = require('q');


function getProfilesRoles(profiles) {
    const d = q.defer();
    let profileObjects = {};

    q.fcall(async () => {

        profiles.forEach(profile => {
            //profileObjects[ne]
        })

    })
}